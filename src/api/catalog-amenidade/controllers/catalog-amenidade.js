'use strict';

/**
 *  catalog-amenidade controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::catalog-amenidade.catalog-amenidade');

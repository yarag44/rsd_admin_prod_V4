'use strict';

/**
 * catalog-amenidade service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::catalog-amenidade.catalog-amenidade');
